enum DigitalInputPins {
    LEFT_TURN = PB0,
    RIGHT_TURN = PB15,
    HAZARDS = PC13,
    BRAKE = PA9,
    REGEN = PC6,
    FORWARD = PB1
};

enum AnalogInputPins {
    THROTTLE = 27,
};

enum DigitalOutputPins {
    BRAKE_LIGHT = 26,
    LEFT_TURN_LIGHT = 8,
    RIGHT_TURN_LIGHT = 9,
    DRO = 25,
    BMS_STROBE = 16
};